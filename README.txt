Aminals

A plug-in project by matt fortunati and ed gallyot.

To build:

CMake is the build system.

Debug:
cmake --preset dbg . && cmake --build --preset dbg

Debug:
cmake --preset rls . && cmake --build --preset rls
